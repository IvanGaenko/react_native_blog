// Imports
import { StyleSheet } from 'react-native';

// UI Imports
import { blockPadding } from '../../../ui/responsive';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'column',
    padding: blockPadding,
  },
});
