// UI Imports
import { invisible, inputColor } from '../../../ui/colors';
import { blockPadding } from '../../../ui/responsive';

export default {
  searchIcon: {
    size: 25,
    containerStyle: {
      padding: 10,
    },
  },
  inputContainerStyle: {
    backgroundColor: inputColor,
  },
  containerStyle: {
    backgroundColor: invisible,
    padding: blockPadding,
  },
};
